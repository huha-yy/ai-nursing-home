"""Shipped flow definitions — the ONLY package code_refs may load from (§11).

Vendor-authored, Reviewer-gated flows land here (the HR-onboarding pilot in
P13c). Each flow version stays importable while any non-terminal run pins it
(spec §9 version retention)."""
